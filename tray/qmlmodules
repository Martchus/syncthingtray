qt_add_qml_module(${META_TARGET_NAME}
    URI "Main"
    VERSION 1.0
    NO_PLUGIN
    QML_FILES Main.qml
)

add_subdirectory(content)
add_subdirectory(imports)

target_link_libraries(${META_TARGET_NAME} PRIVATE
    ${META_PROJECT_VARNAME}Contentplugin
    ${META_PROJECT_VARNAME}CustomControlsplugin
    ${META_PROJECT_VARNAME}plugin
)
